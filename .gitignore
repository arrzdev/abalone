# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-ssr/
build/
.next/
out/
.out/
*.tsbuildinfo

# Vite prebundle caches (any app using Vite SSR)
.vite/
**/.vite/

# TanStack Router / Start generated files
.tanstack/
**/.tanstack/
**/routeTree.gen.ts

# nativ generated route files (stamped from nativ.config.ts)
**/__root.gen.tsx
**/router.gen.tsx

# Turbo
.turbo/
**/.turbo/
*.turbo.log
.turbo.json

# Environment files — secrets live under apps/*/env/ (see env/.env.example)
**/env/.env
**/env/.env.*
!**/env/.env.example

# Same filenames at app root or elsewhere (legacy / other tools)
.env
.env.local
.env.*.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env.staging.local

# IDE
.vscode/*
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
*.log

# Dev log sink output (see @repo/dev/log-sink)
.dev-logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
turbo.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Vitest json reporter (CI deployment summary)
**/test-results.json

# Playwright (E2E / self-test harness)
**/test-results/
**/playwright-report/
**/blob-report/
**/playwright/.cache/

# Coverage directory used by tools like istanbul
coverage/
*.lcov
.nyc_output

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache
.cache
.parcel-cache

# Temporary files
*.tmp
*.temp
cookies.txt

# Lock files (pnpm is canonical; ignore others so they don’t reappear)
package-lock.json
yarn.lock

# Cloudflare Workers
**/.wrangler/
**/.stripe-secret-ready

# TypeScript project-reference emit (generated)
**/.tsbuild/

# Cursor debug logs (keep .cursor config committed)
.cursor/debug*.log
.cursor/*.log
.cursor-screenshots/

# Misc
*.local