# `@repo/dev`

Local development helpers (ports, Stripe CLI webhook redirect, wrangler cron simulation). Sources live under **`src/`**; import from subpaths such as **`@repo/dev/clear-ports`**.

## Related

- **`@repo/env-validation`** — env registry, file parsing, validation
