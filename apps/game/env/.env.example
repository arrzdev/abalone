# The game reads no environment. This file exists because it is the contract
# CI and the deploy seed `env/.env` from — the first variable this app needs is
# declared in `schema.ts` and given a placeholder here, in the same change.
